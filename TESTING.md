# Чеклист тестирования

Планировщик детского сна: серверная часть (Spring Boot / Kotlin REST API,
авторизация через сессию) и клиентская часть (`index.html` — расчёт, таймлайн,
история целиком в браузере). Ниже — раздельные чеклисты.

Автотесты бэкенда: [`ApiFlowTest`](src/test/kotlin/com/sleepplanner/ApiFlowTest.kt)
(Kotest FeatureSpec, контекст на in-memory H2). Запуск: `./gradlew test`.

---

## Бэкенд (REST API)

Помечено `[авто]` — покрыто в `ApiFlowTest`; `[ручн.]` — проверяется вручную/требует доработки.

### `POST /api/register`
- [x] `[авто]` Успешная регистрация → `200` + `{username}`, в сессии `uid`/`uname`
- [x] `[авто]` Логин нормализуется: `trim` + `lowercase`
- [x] `[авто]` Логин < 3 символов → `400` «Логин — минимум 3 символа»
- [x] `[авто]` Пароль < 4 символов → `400` «Пароль — минимум 4 символа»
- [x] `[авто]` Занятый логин (с учётом нормализации) → `409` «Такой логин уже занят»
- [x] `[авто]` В БД только BCrypt-хеш, не открытый пароль
- [ ] `[ручн.]` `username`/`password` = `null` → корректная ошибка `400`, без `500`

### `POST /api/login`
- [x] `[авто]` Верные данные → `200` + `{username}`, сессия установлена
- [x] `[авто]` Нормализация: вход с другим регистром/пробелами находит пользователя
- [x] `[авто]` Неверный пароль → `401` «Неверный пароль»
- [x] `[авто]` Несуществующий пользователь → `404` «Пользователь не найден»

### `POST /api/logout` · `GET /api/me`
- [x] `[авто]` `logout` инвалидирует сессию; затем `/me` без `username`
- [x] `[авто]` `/me` без сессии → `200` с пустым `username` (не `401`)

### `GET /api/history`
- [x] `[авто]` Без сессии → `401` «Требуется вход»
- [x] `[авто]` Сортировка по дате по убыванию
- [x] `[авто]` Поля в snake_case (`nap_count`, `naps_json`, `actual_wake_m`, …)
- [x] `[авто]` Изоляция по `user_id` — только свои записи
- [x] `[авто]` При запросе срабатывает очистка записей старше 90 дней

### `POST /api/history`
- [x] `[авто]` Без сессии → `401`
- [x] `[авто]` Без `date` → `400` «Не указана дата»
- [x] `[авто]` Первое сохранение → `{updated:false}`, повторное за дату → `{updated:true}` (upsert, без дубля)
- [x] `[авто]` Пустой `an` → сохраняется как `null` (`actual_night`)
- [x] `[авто]` Кавычки внутри `naps` вырезаются, JSON остаётся валидным
- [ ] `[ручн.]` Частично заполненное тело (nullable-поля) не вызывает `500`
- [ ] `[ручн.]` Битый JSON → `400`, не `500`

### `DELETE /api/history/{date}`
- [x] `[авто]` Без сессии → `401`
- [x] `[авто]` Удаление существующей записи → `200 {ok:true}`, исчезает из списка
- [x] `[авто]` Удаление несуществующей даты → `200` без ошибки
- [x] `[авто]` Нельзя удалить чужую запись (фильтр по `user_id`)

### Retention / очистка 90 дней
- [x] `[авто]` Запись `date < now-90` удаляется; граничная (`=cutoff`) остаётся
- [x] `[авто]` Очистка затрагивает только текущего пользователя (через изоляцию)

### Безопасность / инфраструктура
- [ ] `[ручн.]` Cookie `JSESSIONID` httponly; `Secure` при `SESSION_COOKIE_SECURE=true`
- [ ] `[ручн.]` `SameSite=Lax` в cookie
- [ ] `[ручн.]` Параметризованные запросы JPA (нет SQL-инъекций через `date`/`username`)
- [ ] `[ручн.]` CORS на `/api/**` с `allowCredentials=true`
- [ ] `[ручн.]` Конфиг через env (`JDBC_DATABASE_URL`, `JPA_DDL_AUTO`, `PORT`)

---

## Фронтенд (`index.html` · клиентская логика)

Всё проверяется вручную в браузере (логика расчёта и рендер — на клиенте).

### Экран авторизации
- [ ] Переключение Вход ↔ Регистрация меняет заголовок, подпись, кнопку, ссылку
- [ ] В режиме регистрации появляется поле «Повторите пароль»
- [ ] Пустой логин/пароль → «Введите логин и пароль»
- [ ] Логин < 3 символов → клиентская ошибка до запроса
- [ ] Регистрация: пароль < 4 → ошибка; пароли не совпадают → «Пароли не совпадают»
- [ ] Серверные ошибки (409/401/404) отображаются в `authMsg`
- [ ] Успешный вход/регистрация скрывает auth-экран, показывает имя в шапке
- [ ] При загрузке сессия восстанавливается через `GET /me` (автологин)
- [ ] «Выйти» очищает состояние, поле пароля, возвращает на auth-экран

### Параметры дня и расчёт
- [ ] Ползунок «общее бодрствование» 8–16 ч, шаг 0.5; подпись без `.0`
- [ ] Ползунок «между снами» 3–8 ч, шаг 0.5; подпись обновляется
- [ ] Любое изменение поля мгновенно пересчитывает (`oninput`)
- [ ] Смена «кол-во снов» (1–4) перестраивает строки снов с дефолтами
- [ ] При перестроении сохраняются уже введённые времена снов
- [ ] «Очистить поля» сбрасывает к дефолтам (12ч/6ч/07:00/1 сон/пусто) + toast

### Валидация расчёта (баннер)
- [ ] Нет снов → «Добавьте хотя бы один дневной сон», результаты тускнеют
- [ ] Первый сон ≤ утреннего подъёма → ошибка
- [ ] Пробуждение ≤ засыпания внутри сна → ошибка с номером сна
- [ ] Сон начинается раньше пробуждения предыдущего → ошибка
- [ ] Расчёт выходит за 24 ч → ошибка, `last=null`
- [ ] При ошибке «Сохранить» показывает «Сначала исправьте параметры»

### Результаты
- [ ] Карточки снов: метка по порядку, факт vs рекомендация
- [ ] Карточка «ночь»: время и мета (кол-во снов, Σ сна, вечернее окно)
- [ ] Карточка «факт. бодрствование» пустая без `an`, заполняется при указании
- [ ] Дельта плана («больше/меньше/точно по плану») верна
- [ ] Статы (до 1-го сна / Σ дневного / после сна / ночной сон) совпадают
- [ ] Переход суток (ночь после полуночи) — корректные времена

### Таймлайн
- [ ] 24-часовая шкала с метками каждые 3 часа
- [ ] Сегменты бодрствование/дневной/ночной в правильных цветах и пропорциях
- [ ] Маркеры: подъём, каждый сон, ночь, реком. первый сон при расхождении ≥5 мин
- [ ] Ночь за полночь разбивается на два сегмента
- [ ] При невалидных данных — заглушка «Исправьте параметры»

### История (UI)
- [ ] Сохранение → toast «Сохранено» / «Запись за DD/MM/YYYY обновлена»
- [ ] Список: дата `DD/MM/YYYY`, кол-во снов, план, времена
- [ ] Метрики средних за 7 и 30 дней; «нет записей за период» при отсутствии
- [ ] Среднее берёт факт. бодрствование где есть, иначе план (`wh*60`)
- [ ] Клик по записи подставляет поля, строит сны, пересчитывает, скроллит вверх
- [ ] ✕ удаляет запись (не триггерит загрузку), список обновляется
- [ ] Пустая история → плашка «Пока нет сохранённых дней»
- [ ] Toast появляется и исчезает (~2.4с)

### Сетевой слой
- [ ] `api()` шлёт `credentials:'same-origin'`, ставит `Content-Type` только при теле
- [ ] Ошибки сервера → `data.error` или «Ошибка сервера (статус)»
- [ ] Потеря сессии (401 на `/history`) не роняет UI
